// swagger.js
const express = require('express');
const router = express.Router();

// Placeholder response
router.get('/', (req, res) => {
  res.send('Swagger docs placeholder');
});

module.exports = router;
